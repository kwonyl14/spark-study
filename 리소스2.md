## 2. Kubernetes 주요 리소스 현황

IAS는 Kubernetes 환경에서 API Server와 Spark 분석 Job을 실행하며, 외부 접근 및 Prometheus 모니터링을 위해 Ingress, Service, ServiceMonitor 등의 리소스를 함께 사용하고 있다.

### 2.1 IAS API Server 관련 리소스

| Kind | 리소스 | 역할 | 상태/비고 |
|---|---|---|---|
| Deployment | IAS API Server Deployment | Spring Boot 기반 IAS API Server Pod 실행 및 배포 상태 관리 | 운영 중 |
| Ingress | IAS API Ingress | UI에서 IAS API Server로 접근하기 위한 HTTP(S) 진입점 | 운영 중 |
| Service | IAS API Service | Ingress 및 Kubernetes 내부에서 IAS API Server Pod에 접근하기 위한 Endpoint 제공 | 운영 중 |
| ServiceAccount | IAS API ServiceAccount | IAS API Server가 Kubernetes API를 사용할 때 사용하는 Identity | 운영 중, 실제 RBAC 권한 확인 필요 |
| ServiceMonitor | IAS API ServiceMonitor | Prometheus가 IAS API Server의 Metric을 수집할 수 있도록 Scrape 대상을 정의 | 운영 중, 실제 Prometheus 연결 설정 확인 필요 |

구조는 다음과 같다.

```text
UI / Client
     │
     ▼
  Ingress
     │
     ▼
IAS API Service
     │
     ▼
IAS API Server Deployment
     │
     └─ API Server Pod
           │
           ├─ ServiceAccount 사용
           │
           └─ SparkApplication CR 생성
```

API Server Metric 수집은 현재 구성상 다음과 같은 구조로 판단된다.

```text
IAS API Server
     │
     │ /metrics
     ▼
IAS API Service
     │
     ▼
ServiceMonitor
     │
     ▼
Prometheus
     │
     ▼
Grafana
```

실제 Metric Endpoint, ServiceMonitor Selector 및 Prometheus 연결 여부는 현재 Kubernetes Manifest와 Prometheus 설정을 통해 확인이 필요하다.

### 2.2 Spark 분석 Job 관련 리소스

IAS API Server는 분석 요청을 받으면 Spark Operator가 처리할 SparkApplication Custom Resource를 동적으로 생성한다.

| Kind | 리소스 | 역할 | 상태/비고 |
|---|---|---|---|
| SparkApplication | IAS Spark Analysis Job | regression, clustering, tttmpca, tttmrf 등의 PySpark 분석 실행 정의 | API Server가 요청마다 동적 생성 |
| Pod | Spark Driver | Spark Job 제어 및 PySpark 분석 코드 실행 | Spark Operator가 생성 |
| Pod | Spark Executor | Spark 분산 연산 수행 | Spark Job 실행 중 생성 |
| Spark Operator | Spark Operator | SparkApplication CR을 감시하고 Driver/Executor Pod Lifecycle 관리 | 플랫폼에 구축됨 |

API Server의 ServiceAccount는 SparkApplication 생성 등 Kubernetes API 접근 권한을 부여하기 위해 사용되는 것으로 추정된다.

정확한 권한 범위는 다음 RBAC 리소스를 확인해야 한다.

```text
ServiceAccount
    │
    ▼
Role / ClusterRole
    │
    ▼
RoleBinding / ClusterRoleBinding
```

### 2.3 Pushgateway 관련 리소스

Spark 분석 Job에서 발생하는 일부 Metric은 Pushgateway로 전달하고 있다.

단발성으로 실행되고 종료되는 Spark Job의 Metric을 Pushgateway가 전달받고, Prometheus가 Pushgateway의 Metric을 Scrape하는 구조이다.

| Kind | 리소스 | 역할 | 상태/비고 |
|---|---|---|---|
| Deployment | Pushgateway Deployment | Spark 분석 Job에서 Push하는 Metric을 수신하는 Pushgateway 실행 | 운영 중 |
| Service | Pushgateway Service | Spark Job 및 Prometheus가 Pushgateway에 접근할 수 있도록 Endpoint 제공 | 운영 중 |
| ServiceMonitor | Pushgateway ServiceMonitor | Prometheus가 Pushgateway Metric을 Scrape하도록 대상 정의 | 운영 중, 실제 Selector 연결 확인 필요 |

Metric 흐름은 다음과 같다.

```text
Spark Analysis Job
        │
        │ Metric Push
        ▼
   Pushgateway
        │
        ▼
Pushgateway Service
        │
        ▼
  ServiceMonitor
        │
        ▼
    Prometheus
        │
        ▼
      Grafana
```

따라서 현재 IAS 환경에서는 Metric 수집 방식이 크게 두 가지로 구분된다.

```text
IAS API Server
    │
    └─ Prometheus가 직접 Scrape

Spark Analysis Job
    │
    └─ Pushgateway로 Metric Push
             │
             └─ Prometheus가 Pushgateway를 Scrape
```

### 2.4 주요 Kubernetes 리소스 요약

| 영역 | Kind | 주요 목적 |
|---|---|---|
| IAS API | Deployment | API Server Pod 실행 |
| IAS API | Ingress | UI 요청을 API Server로 전달 |
| IAS API | Service | API Server 접근 Endpoint 제공 |
| IAS API | ServiceAccount | API Server의 Kubernetes API 접근 Identity |
| IAS API Monitoring | ServiceMonitor | API Server Metric Scrape 대상 정의 |
| Spark Analysis | SparkApplication | PySpark 분석 Job 실행 정의 |
| Spark Analysis | Pod | Spark Driver/Executor 실행 |
| Metric | Deployment | Pushgateway 실행 |
| Metric | Service | Pushgateway 접근 Endpoint 제공 |
| Metric | ServiceMonitor | Pushgateway Metric Scrape 대상 정의 |

> ServiceMonitor는 Kubernetes 기본 리소스가 아니라 Prometheus Operator가 제공하는 Custom Resource이다. IAS의 Kubernetes 모니터링 구성을 설명하는 데 필요한 리소스이므로 본 현황에 포함한다.
