2. Kubernetes 주요 리소스 현황

IAS는 Kubernetes 환경에서 API Server와 Spark 분석 Job을 실행하며, 외부 접근과 Prometheus 모니터링을 위해 Ingress, Service, ServiceMonitor 등의 리소스를 함께 사용하고 있다.

2.1 IAS API Server 관련 리소스

Kind| 리소스| 역할| 상태/비고
"Deployment"| IAS API Server Deployment| Spring Boot 기반 IAS API Server Pod를 실행하고 replica 및 배포 상태를 관리| 운영 중
"Ingress"| IAS API Ingress| UI 등 외부/상위 서비스에서 IAS API Server로 접근하기 위한 HTTP(S) 진입점| 운영 중
"Service"| IAS API Service| Ingress 및 Kubernetes 내부에서 IAS API Server Pod에 접근할 수 있도록 네트워크 Endpoint 제공| 운영 중
"ServiceAccount"| IAS API ServiceAccount| IAS API Server Pod가 Kubernetes API를 사용할 때 사용하는 identity| 운영 중, 실제 부여 권한은 RBAC 설정 확인 필요
"ServiceMonitor"| IAS API ServiceMonitor| Prometheus Operator가 IAS API Server의 Service를 대상으로 metrics endpoint를 scrape하도록 정의| 운영 중, 실제 Prometheus 연결 설정 확인 필요

구조를 단순화하면 다음과 같다.

UI / Client
     │
     ▼
  Ingress
     │
     ▼
IAS API Service
     │
     ▼
IAS API Server Deployment
     │
     └─ Pod
          │
          ├─ ServiceAccount 사용
          │
          └─ SparkApplication CR 생성

모니터링 경로는 현재 구성 의도상 다음과 같은 구조로 보인다.

IAS API Server
     │
     │ /metrics 등
     ▼
IAS API Service
     │
     ▼
ServiceMonitor
     │
     ▼
Prometheus
     │
     ▼
Grafana

단, 실제 metrics endpoint, ServiceMonitor selector 및 Prometheus가 해당 ServiceMonitor를 선택하고 있는지는 현재 manifest 및 Prometheus 설정을 통해 확인이 필요하다.

2.2 Spark 분석 Job 관련 리소스

IAS API Server는 분석 요청을 받으면 Spark Operator가 처리할 "SparkApplication" Custom Resource를 동적으로 생성한다.

Kind| 리소스| 역할| 상태/비고
"SparkApplication"| IAS Spark Analysis Job| regression, clustering, tttmpca, tttmrf 등의 PySpark 분석 실행 정의| API Server가 요청마다 동적 생성
Spark Driver Pod| Spark Driver| 분석 Job 제어 및 PySpark 코드 실행| Spark Operator가 생성
Spark Executor Pod| Spark Executor| Spark 분산 연산 수행| Spark Job 실행 중 동적 생성
Spark Operator| Spark Operator| SparkApplication CR을 감시하고 Driver/Executor Pod lifecycle 관리| 플랫폼에 구축됨

API Server의 "ServiceAccount"가 SparkApplication 생성 등의 Kubernetes API 권한을 갖기 위해 사용되는 것으로 추정되며, 정확한 권한 범위는 다음 RBAC 리소스를 확인해야 한다.

ServiceAccount
Role / ClusterRole
RoleBinding / ClusterRoleBinding

2.3 Pushgateway 관련 리소스

Spark 분석 Job에서 발생하는 일부 Prometheus Metric은 장기 실행 서버처럼 Prometheus가 직접 scrape하는 방식이 아니라 Pushgateway로 push하고 있다.

이를 위해 Kubernetes에 Pushgateway 관련 리소스가 별도로 구성되어 있다.

Kind| 리소스| 역할| 상태/비고
"Deployment"| Pushgateway Deployment| Spark 분석 Job에서 전달하는 metric을 수신하는 Pushgateway 실행| 운영 중
"Service"| Pushgateway Service| Spark Job과 Prometheus가 Pushgateway에 접근할 수 있도록 Endpoint 제공| 운영 중
"ServiceMonitor"| Pushgateway ServiceMonitor| Prometheus Operator가 Pushgateway의 metric을 scrape하도록 정의| 운영 중, 실제 selector 연결 확인 필요

Metric 흐름은 다음과 같다.

Spark Analysis Job
        │
        │ push
        ▼
   Pushgateway
        │
        │ expose metrics
        ▼
Pushgateway Service
        │
        ▼
  ServiceMonitor
        │
        ▼
    Prometheus
        │
        ▼
      Grafana

즉 IAS 환경의 Prometheus metric 수집은 크게 두 가지 방식이 존재한다.

1. IAS API Server
   Prometheus가 직접 Pull/Scrape

2. 단발성 Spark Analysis Job
   Spark Job → Pushgateway로 Push
             → Prometheus가 Pushgateway를 Scrape

2.4 주요 Kubernetes 리소스 요약

영역| Kind| 주요 목적
IAS API| "Deployment"| API Server Pod 실행
IAS API| "Ingress"| UI/외부 요청 진입
IAS API| "Service"| API Server 네트워크 Endpoint
IAS API| "ServiceAccount"| Kubernetes API 접근 identity
IAS API Monitoring| "ServiceMonitor"| API Server metric Prometheus scrape 설정
Spark Analysis| "SparkApplication"| 분석 Job 실행 정의
Spark Analysis| "Pod"| Driver/Executor 실행
Metric Push| "Deployment"| Pushgateway 실행
Metric Push| "Service"| Pushgateway 접근 Endpoint
Metric Push| "ServiceMonitor"| Pushgateway metric Prometheus scrape 설정

"ServiceMonitor"는 Kubernetes 기본 Kind가 아니라 Prometheus Operator가 제공하는 Custom Resource이지만, IAS의 Kubernetes 운영 및 모니터링 구조를 설명하는 데 필요한 리소스이므로 본 현황에 포함한다.
