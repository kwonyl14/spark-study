Multi-Agent Debate Observatory

Internal LLM / Claude Code / WSL Edition

너는 이 프로젝트의 Principal Architect이자 Lead Engineer다.

WSL에서 실행되는 사내 LLM 기반 Multi-Agent Debate Web Application을 처음부터 설계하고 구현한다.

여러 익명 AI Agent가 하나의 문제에 대해 독립적으로 의견을 제시하고, 상호 비판·방어·수정·대안 제시를 수행한 뒤 별도의 Judge가 최종 결과를 판정한다.

사용자는 이 전체 과정을 픽셀 캐릭터들이 회의실에서 토론하는 웹 UI를 통해 실시간으로 관찰한다.

이 프로젝트는 단순 Group Chat이 아니다.

핵심 흐름은:

Independent Proposal
        ↓
Cross Critique
        ↓
Defense / Revision
        ↓
Alternative
        ↓
Final Position
        ↓
Independent Judge
        ↓
Final Decision

이다.

---

1. 개발 방식

전체 시스템을 한 번에 만들지 않는다.

아래 Phase 단위로 개발한다.

내가 명시적으로:

Phase N 시작

이라고 지시하기 전에는 다음 Phase로 넘어가지 않는다.

각 Phase 완료 후 다음을 보고하고 멈춘다.

1. 구현 내용
2. 주요 변경 파일
3. 실행 방법
4. 테스트 결과
5. 발견된 문제
6. 다음 Phase 위험요소

---

2. 실행 환경

주 실행환경:

Windows
└─ WSL Linux
   ├─ Claude Code CLI
   ├─ Internal LLM Configuration
   └─ Debate Application

Frontend와 Backend 모두 WSL에서 실행한다.

사용자는 Windows 브라우저에서 예를 들어:

http://localhost:3000

으로 접속한다.

Windows에 별도 Claude Runtime을 요구하지 않는다.

Docker는 필수가 아니다.

WSL Native 실행을 최우선으로 한다.

가능하면:

./scripts/setup.sh
./scripts/start.sh

정도로 실행 가능하게 한다.

---

3. 사내 LLM 환경

현재 Claude Code CLI는 Public Anthropic API가 아니라 사내 LLM Gateway에 연결되어 있다.

개념:

Claude Code CLI
       ↓
Claude Model Alias
       ↓
Internal LLM Gateway
       ↓
DeepSeek / GLM / 기타 사내 모델

실제:

- Claude Code 버전
- CLI 옵션
- 설정 파일
- Model Alias
- 사내 모델 Mapping
- 환경변수

등은 현재 WSL 환경을 직접 확인한다.

알지 못하는 CLI option이나 설정 경로를 추측하여 하드코딩하지 않는다.

기존 Claude Code 설정을 가능한 한 그대로 사용한다.

---

4. 외부 Provider 제외

현재 구현 범위에서는 다음을 필요로 하지 않는다.

- Codex
- OpenAI API
- Anthropic Public API
- OpenRouter
- Gemini
- 기타 외부 LLM SaaS

향후 확장을 위한 Adapter 구조는 유지해도 되지만 현재 구현의 핵심 Provider는:

Internal Claude Code CLI

하나다.

---

5. 핵심 Architecture

Web Frontend
     │
 HTTP / SSE
     │
     ▼
Backend
     │
Debate Engine
     │
Orchestrator
     │
Agent Adapter
     │
InternalClaudeCliAdapter
     │
subprocess
     │
Claude Code CLI
     │
Internal LLM Gateway

다음 경계를 유지한다.

Frontend
API
Debate Engine
Context Builder
Agent Adapter
Storage
Event System

Frontend가 LLM을 직접 호출하지 않는다.

Debate Engine이 Claude CLI command 세부사항을 알아서는 안 된다.

---

6. Model / Agent 구조

다음을 분리한다.

ModelProfile
Participant
AgentRun
Judge

ModelProfile 예:

id
display_name
cli_model_alias
enabled

사용자는 각 Participant마다 원하는 사내 ModelProfile을 선택할 수 있어야 한다.

예:

Participant A → GLM
Participant B → DeepSeek
Participant C → GLM

Judge → DeepSeek

동일 모델을 여러 Agent가 사용해도 된다.

각 Agent invocation은 독립 Context를 사용한다.

---

7. Participant 수

Debater 수를 고정하지 않는다.

Judge 제외 최소:

2 Participants

를 요구한다.

예:

2 Debaters + Judge
3 Debaters + Judge
5 Debaters + Judge
...

Architecture는 N명을 지원한다.

실제 Resource 보호를 위해:

max_participants
max_concurrent_agent_runs

를 설정값으로 둘 수 있다.

3명 기준으로 로직이나 UI를 하드코딩하지 않는다.

---

8. Agent 익명성

Agent들은 다른 Participant의 실제 Model을 알 수 없다.

Backend에서는:

Participant A → GLM
Participant B → DeepSeek

를 알고 있어도 된다.

하지만 Agent에게 전달되는 것은:

Participant A
Participant B

뿐이다.

다른 Agent에게 다음을 노출하지 않는다.

- 실제 모델명
- Model Alias
- Gateway 정보
- Provider
- 실행 설정

Judge 역시 Participant의 실제 모델명을 알 필요가 없다.

사용자는 설정/상세 UI에서 실제 Model Mapping을 볼 수 있다.

---

9. Orchestrator

다음은 LLM이 아니라 deterministic code가 처리한다.

- Round 진행
- Agent 호출
- 병렬 실행
- synchronization
- timeout
- retry
- cancellation
- failure handling
- concurrency 제한
- persistence
- event 발생
- Debate 완료

LLM Moderator는 MVP 필수가 아니다.

---

10. Debate Protocol

Round 0 — Problem Definition

사용자 질문을 다음 구조로 정리한다.

question
constraints
success_criteria
uncertainties

---

Round 1 — Independent Proposal

모든 Participant가 서로의 답변을 보지 않고 독립적으로 답한다.

가능하면 병렬 실행한다.

---

Round 2 — Cross Critique

다른 Participant들의 공개 Proposal을 익명으로 읽고 공격한다.

검토:

weak arguments
hidden assumptions
logical flaws
failure cases
missing alternatives
trade-offs

Participant 수가 커질 경우 N² Critique 문제가 발생하므로 향후 targeted critique가 가능하도록 설계한다.

---

Round 3 — Defense / Revision

Agent는 비판을 읽고:

defend
partially_accept
revise
withdraw

중 적절한 대응을 한다.

입장 수정은 정상적인 Debate 결과다.

---

Round 4 — Alternative / Improvement

기존 주장의 단순 방어가 아니라:

- 개선안
- 절충안
- 새로운 대안
- 다른 의견을 결합한 안

을 제시한다.

---

Round 5 — Final Position

각 Participant가 다음을 제출한다.

recommendation
key_reasons
accepted_critiques
remaining_risks
remaining_disagreements
confidence

---

11. Judge

Judge는 모든 공개 Debate 내용을 보고 최종 판단한다.

단순 다수결을 금지한다.

Judge는 다음을 평가한다.

argument quality
technical correctness
counterargument survival
trade-offs
failure conditions
position revisions
unresolved issues

결과:

final_recommendation
reasoning_summary
strongest_arguments
rejected_alternatives
remaining_risks
unresolved_issues
confidence

내부 chain-of-thought를 노출하지 않는다.

---

12. Shared Debate State

Agent끼리 직접 통신하지 않는다.

Agent
 ↓
Debate Engine
 ↓
Structured Message
 ↓
Shared Debate State
 ↓
Other Agents

Agent에게 전달 가능한 것:

- 사용자 질문
- 현재 Round
- 다른 Agent의 공개 발언
- Active Issue
- unresolved Issue
- 자신의 이전 입장

다음은 공유하지 않는다.

chain-of-thought
hidden reasoning
Claude debug output
shell log
system prompt
environment variables
credential
gateway information
temporary scratchpad

---

13. Structured Message / Issue

발언은 구조화한다.

예:

{
  "agent_id": "participant-b",
  "round": 2,
  "type": "critique",
  "target_agent_id": "participant-a",
  "issue_id": "issue-3",
  "content": "...",
  "summary": "..."
}

Message Type 예:

proposal
critique
defense
revision
alternative
final_position
judge_result

핵심 논쟁은 Issue Entity로 관리한다.

상태:

OPEN
CONTESTED
RESOLVED
UNRESOLVED

단순히 Chat Message만 쌓는 시스템으로 만들지 않는다.

---

14. InternalClaudeCliAdapter

Adapter가 담당한다.

- Claude CLI subprocess 실행
- ModelProfile 적용
- Prompt 전달
- stdout 수집
- stderr 분리
- timeout
- cancellation
- exit code 처리
- response parsing
- structured response normalization
- error normalization

Raw CLI output 전체를 Debate UI에 표시하지 않는다.

Claude Process
    ↓
Adapter
    ↓
Parse / Normalize
    ↓
AgentResponse
    ↓
Debate Message

---

15. CLI 실제 환경 확인

Adapter 구현 전에 현재 설치된 Claude Code CLI를 확인한다.

특히:

version
executable path
non-interactive invocation
prompt input
model alias selection
output format
JSON/structured output
concurrent execution
timeout/cancellation

을 실제 환경에서 확인한다.

추측해서 구현하지 않는다.

---

16. Context Builder

Participant 수와 Round가 증가하면 Context가 폭증한다.

별도 Context Builder를 둔다.

original_question
current_round
current_issue
relevant_messages
agent_previous_position
debate_summary

Debate Engine 코드 곳곳에서 Prompt 문자열을 직접 조립하지 않는다.

---

17. Failure Handling

AgentRun 상태 예:

QUEUED
RUNNING
COMPLETED
TIMEOUT
FAILED
CANCELLED
SKIPPED

Agent 하나가 실패해도 가능한 경우 Debate를 계속한다.

Judge에게 실패 사실은 알려주되 존재하지 않는 의견을 추측하지 않는다.

초기 retry 기본값은 보수적으로 설정한다.

---

18. Realtime Event

Backend → Frontend 실시간 전달은 SSE를 우선 검토한다.

Event 예:

DEBATE_STARTED

ROUND_STARTED
ROUND_COMPLETED

AGENT_STARTED
AGENT_MESSAGE
AGENT_COMPLETED
AGENT_FAILED

ISSUE_CREATED
ISSUE_UPDATED

JUDGE_STARTED
JUDGE_RESULT

DEBATE_COMPLETED

---

19. 매우 중요한 UI Presentation 규칙

Debate Engine의 논리적 진행 속도와 사용자가 보는 UI 재생 속도를 분리한다.

Backend에서 다음 Agent나 다음 Round가 이미 진행 중이더라도 Frontend가 이벤트를 즉시 모두 화면에 소비하면서 기존 발언을 사라지게 만들면 안 된다.

Frontend에는 별도의:

Presentation Queue

또는 동등한 메커니즘을 둔다.

Debate Event는 실시간으로 수신하되 사용자의 화면에서는 읽을 수 있는 속도로 순차 재생한다.

---

20. Speech Bubble 출력 속도

Agent가 발언하면 말풍선을 즉시 전체 출력하지 않는다.

텍스트는 1초에 약 10글자 속도로 표시한다.

즉:

10 characters / second

를 기본값으로 한다.

예:

100글자
→ 약 10초 동안 출력

한 번에 여러 글자를 갱신해도 되지만 체감 속도가 약 10 characters/sec가 되도록 한다.

설정값으로 분리한다.

예:

speech_chars_per_second = 10

---

21. Speech Bubble 유지시간

말풍선 전체 내용이 화면에 표시된 후 즉시 다음 발언으로 넘어가지 않는다.

완전히 출력된 상태에서 최소:

5초

유지한다.

설정 예:

speech_hold_seconds = 5

즉 발언 하나의 화면 재생은:

Typing
    ↓
Full Text Visible
    ↓
Wait 5 Seconds
    ↓
Next Presentation Event

순서다.

---

22. 마지막 Agent 발언 보호

특히 Round의 마지막 Agent 발언이 잘리는 현상을 절대 허용하지 않는다.

예를 들어 Round 1에서:

Participant A
Participant B
Participant C

순서로 보여주는 경우 Participant C의 발언은 반드시:

말풍선 생성
↓
10자/초로 전체 출력 완료
↓
5초 유지
↓
그 이후 Round 2 화면 전환

순서를 지켜야 한다.

Backend에서 이미 Round 2가 시작되었더라도 UI는 이 Presentation Sequence가 끝날 때까지 Round 1의 시각적 표시를 유지할 수 있다.

---

23. Round Transition 규칙

다음처럼 구현하면 안 된다.

ROUND_STARTED event 수신
→ 즉시 현재 말풍선 제거
→ 다음 Round UI

대신:

Backend Event Stream
        ↓
Frontend Event Buffer
        ↓
Presentation Queue
        ↓
현재 Speech 완료 확인
        ↓
5초 Hold 완료
        ↓
Round Transition 표시
        ↓
다음 Agent Speech

형태를 사용한다.

"logical_current_round"와 "displayed_round"를 필요하면 별도로 관리한다.

---

24. Speech Queue

동시에 여러 Agent 응답이 완료될 수 있다.

특히 Round 1에서 Agent를 병렬 실행하면:

A completed
B completed
C completed

이 거의 동시에 발생할 수 있다.

UI에서는 세 말풍선을 동시에 생성했다가 사라지게 하지 않는다.

완료된 공개 발언을 Queue에 넣고 하나씩 순차적으로 보여준다.

예:

Presentation Queue

1. Participant B speech
2. Participant A speech
3. Participant C speech
4. Round transition

실제 완료 순서 또는 deterministic display order 중 하나를 일관되게 선택한다.

---

25. Pixel Meeting Room

메인 UI는 픽셀 회의실이다.

Participant 수에 맞게 좌석을 동적으로 배치한다.

        Participant B

Participant A     Participant C

            TABLE

Participant D     Participant E

             Judge

Judge는 일반 Participant와 시각적으로 구분한다.

---

26. Agent 표시 상태

예:

IDLE
QUEUED
THINKING
SPEAKING
WAITING
AGREEING
DISAGREEING
ERROR
DONE

현재 말하는 Agent가 명확하게 보이게 한다.

Speech Bubble에는 전체 장문 대신 핵심 발언을 보여줄 수 있다.

전체 공개 내용은 Transcript에 유지한다.

---

27. 화면만 봐도 진행상황을 이해할 수 있어야 한다

사용자가 Transcript를 읽지 않고 Pixel Meeting Room만 보고 있어도 최소한 다음을 이해할 수 있어야 한다.

- 현재 Round
- 현재 말하는 Agent
- 어떤 Agent가 기다리는지
- 어떤 Agent가 생각 중인지
- 누가 동의/반대했는지
- Round가 언제 종료되는지
- Judge 단계인지

화면 진행이 너무 빨라 사용자가 발언을 인식하지 못하는 UX를 허용하지 않는다.

Realtime은 “최대한 빨리 표시”라는 의미가 아니라 “실제 Debate 상태를 지연 없이 수신하되 사람이 이해할 수 있는 속도로 표현”한다는 의미다.

---

28. Desktop / Mobile

Desktop에서는:

Debate Info | Pixel Meeting Room | Issues
------------------------------------------
Transcript / Events / Result

형태를 기본으로 검토한다.

Mobile은 최소 360px width를 고려한다.

Header
Pixel Room
Current Speaker
Tabs
[Debate] [Issues] [Agents] [Result]

Desktop UI를 단순 축소하지 않는다.

다음을 고려한다.

- horizontal overflow 금지
- 긴 텍스트 wrapping
- speech bubble 화면 이탈 방지
- touch friendly controls
- dynamic participant count
- portrait / landscape
- responsive layout

---

29. 브라우저 자동제어 금지

현재 Claude Code / Orca 개발 환경에 브라우저 직접 제어 기능이 있다고 가정하지 않는다.

다음을 필수 검증수단으로 사용하지 않는다.

Playwright
Selenium
Puppeteer
browser-use
Chrome automation
GUI browser automation

개발 Agent에게 브라우저 클릭이나 실제 화면 조작을 요구하지 않는다.

Frontend 검증은 가능한 범위에서:

TypeScript check
lint
production build
unit test
component test
state/event test
Presentation Queue test
CSS/layout review

로 수행한다.

실제 Browser에서 눈으로 확인해야 하는 것은:

Manual Browser Verification Required

로 보고한다.

---

30. Presentation Queue 필수 테스트

Frontend 자동 테스트에서 최소 다음 Scenario를 검증한다.

Case 1 — Agent Speech

AGENT_MESSAGE A

수신 후:

1. A가 SPEAKING 상태
2. 텍스트가 점진적으로 표시됨
3. 전체 표시 완료
4. Hold 상태 유지
5. 다음 Event 처리

---

Case 2 — 여러 Agent가 빠르게 완료

AGENT_MESSAGE A
AGENT_MESSAGE B
AGENT_MESSAGE C

가 빠르게 들어와도:

A → B → C

발언이 겹치지 않고 순차 표시되어야 한다.

---

Case 3 — 마지막 Agent + 다음 Round

다음 Event가 즉시 들어와도:

AGENT_MESSAGE C
ROUND_COMPLETED 1
ROUND_STARTED 2

화면에서는:

C typing
→ C full speech
→ 5 sec hold
→ Round 1 종료
→ Round 2

순서를 보장한다.

---

Case 4 — 긴 발언

긴 Speech도 중간에 다음 Event 때문에 제거되지 않는다.

---

31. Storage

초기에는 SQLite를 사용한다.

주요 Entity:

Debate
Participant
ModelProfile
AgentRun
Message
Issue
Decision
Event

Event를 저장하여 향후 History / Replay 기능에 사용할 수 있게 한다.

---

32. Application Log와 Debate Log 분리

Application Log:

process started
process exited
timeout
parser error

Debate Transcript:

proposal
critique
defense
revision
alternative
final_position
judge_result

둘을 섞지 않는다.

---

33. 권장 Stack

Phase 0에서 최종 결정한다.

우선 후보:

Backend:

Python
FastAPI
asyncio
SQLite

Frontend:

React
TypeScript

Realtime:

SSE

초기부터 다음은 넣지 않는다.

Kubernetes
Kafka
Redis
Microservices
Cloud infrastructure
External LLM Provider
Complex Authentication

---

34. Phase 계획

Phase 0 — Architecture & Environment Discovery

본격 구현 전에 WSL 환경과 Claude CLI capability를 조사한다.

확인:

Claude version
CLI path
non-interactive invocation
model alias selection
structured output
concurrent execution
timeout/cancellation
internal model mapping

그리고 다음 설계를 확정한다.

- Architecture
- Debate State Machine
- InternalClaudeCliAdapter
- N-Agent 구조
- Judge
- Context Builder
- Event Contract
- Storage
- Presentation Queue
- Responsive UI

설계 결과를 보고하고 멈춘다.

---

Phase 1 — Mock Debate MVP

실제 LLM 없이 Mock Agent로 구현한다.

구현:

2~N Debaters
1 Judge
Debate State Machine
SQLite
API
SSE
Pixel Meeting Room
Presentation Queue
10 chars/sec Speech
5 sec Hold
Round Transition synchronization
Transcript
Issues
Responsive UI

이 Phase에서 UI timing 문제를 반드시 먼저 검증한다.

---

Phase 2 — Internal Claude CLI Adapter

실제 Claude CLI Agent 하나를 연결한다.

구현:

subprocess
model selection
prompt
stdout/stderr
timeout
cancellation
response parsing

---

Phase 3 — Real Multi-Agent Debate

실제 사내 모델 2개 이상을 연결한다.

구현:

Independent Proposal
Cross Critique
Defense / Revision
Alternative
Final Position
Judge
Concurrency
Failure Isolation

---

Phase 4 — Dynamic Scaling

2 / 3 / 5명 이상의 Debater를 검증한다.

검토:

context growth
process count
critique growth
seat layout
Presentation Queue backlog
latency

---

Phase 5 — Judge / Consensus

추가:

Issue Resolution
Consensus Detection
Unresolved Issue
Confidence
Early Termination

---

Phase 6 — Debate Intelligence

추가:

Context Trimming
Debate Summarization
Duplicate Argument Detection
Targeted Critique
Issue-based Debate

---

Phase 7 — UX Polish

추가:

Pixel animation
speech bubble polish
agreement/disagreement
round transition
participant focus
mobile tabs
responsive polish

Presentation readability를 애니메이션 화려함보다 우선한다.

---

Phase 8 — History / Replay / Metrics

추가:

Debate History
Replay
Latency
Round Duration
Failure Metrics
Token Usage if available

---

35. Definition of Done

각 Phase에서 가능한 범위에서:

WSL Native 실행 가능
Backend test 통과
Frontend type check 통과
Frontend build 성공
자동 테스트 통과
기존 Phase regression 없음
Credential 노출 없음
N-Agent 구조 유지
Judge 별도 유지
README 최신

를 확인한다.

UI 관련 실제 시각 확인이 필요한 내용은 별도의 Manual Verification 항목으로 보고한다.

---

36. 최우선 UX 규칙

다음 현상은 Regression으로 간주한다.

금지 1

마지막 Agent의 말풍선이 전부 표시되기 전에 다음 Round로 넘어가는 것.

금지 2

여러 Agent 말풍선이 너무 빠르게 나타났다 사라져 화면만 보고는 토론 내용을 이해할 수 없는 것.

금지 3

Backend Event 도착 속도가 UI Presentation 속도를 강제로 결정하는 것.

기본 규칙

Speech speed:
약 10 characters / second

After full speech:
5 second hold

Next speech:
이전 speech presentation 완료 후

Next round:
해당 Round 마지막 speech presentation 완료 후

이 값들은 configuration으로 분리한다.

---

37. 첫 번째 작업

이 프롬프트를 받은 즉시 구현하지 마라.

먼저 전체 요구사항을 비판적으로 검토하고 특히 다음을 확인하라.

1. 현재 Claude CLI를 subprocess Agent로 사용할 수 있는가
2. Model Alias별 사내 모델 선택이 가능한가
3. 여러 CLI Agent 동시 실행이 가능한가
4. N-Agent 구조에서 Context가 어떻게 증가하는가
5. Backend Debate Timeline과 Frontend Presentation Timeline을 어떻게 분리할 것인가
6. Event Stream이 빠르게 들어와도 Speech가 유실되지 않도록 어떻게 Queueing할 것인가
7. 마지막 Agent Speech와 Round Transition 순서를 어떻게 보장할 것인가
8. 페이지 새로고침이나 재접속 시 Presentation State를 어떻게 복구할 것인가
9. 현재 요구사항에서 불필요하게 복잡한 부분은 무엇인가

추천 Architecture와 Phase별 구현 방향을 제시하고 멈춰라.

브라우저를 직접 열거나 제어하려고 하지 마라.

내가:

Phase 0 시작

이라고 지시하기 전에는 본격 구현을 시작하지 마라.
